# Elysium — Python >= 3.10, virtualenv recommended.
#
# GPU / training: install a PyTorch build that matches your CUDA *first*
#   https://pytorch.org/get-started/locally/
# then:
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install -e .
#
# Optional dev tools:
#   pip install -r requirements-dev.txt
#
# If `unsloth` fails to install, try an explicit CUDA extra after PyTorch is installed, e.g.:
#   pip install "unsloth[cu124]"   # adjust cu121 / cu118 to your driver/toolkit
#
# Git LFS (data/): after clone run `git lfs install` then `git lfs pull`.

# --- Core (logging, config, schemas, canvas) ---
loguru>=0.7.0
pydantic>=2.4.0
PyYAML>=6.0
numpy>=1.26.0,<3
Pillow>=10.0.0
opencv-python-headless>=4.9.0.80

# --- Annotation tool (pygame UI) ---
pygame>=2.5.0

# --- Session compression ---
rdp>=0.8

# --- ML stack (SFT / RL / inference) ---
torch>=2.4.0
unsloth
transformers>=4.46.0
trl>=0.12.0
datasets>=3.0.0
peft>=0.13.0
accelerate>=0.34.0
huggingface_hub>=0.25.0
safetensors>=0.4.0
bitsandbytes>=0.44.0
sentencepiece>=0.1.99
einops>=0.7.0
tensorboard>=2.15.0
scikit-learn>=1.4.0   # Phase 7.3: AUC for the SigLIP critic
scikit-image>=0.22.0  # Phase 4.1: SSIM-on-bbox reward
